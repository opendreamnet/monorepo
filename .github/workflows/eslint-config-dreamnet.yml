name: "@dreamnet/eslint-config-dreamnet"

on:
  push:
    paths:
      - workspaces/eslint-config-dreamnet/**.js
      - workspaces/eslint-config-dreamnet/**.ts
      - workspaces/eslint-config-dreamnet/**.json

jobs:
  build:
    name: Publish
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install Node.js 14.x
        uses: actions/setup-node@v1
        with:
          node-version: "14"

      - name: Setup
        working-directory: workspaces/eslint-config-dreamnet
        run: yarn install

      - name: Publish
        working-directory: workspaces/eslint-config-dreamnet
        run: yarn npm publish --access public --tolerate-republish
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
