name: "@dreamnettech/eslint-config-dreamnet"
on:
  push:
    paths:
      - workspaces/eslint-config-dreamnet/**

jobs:
  build:
    name: Publish
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install Node.js 12.x
        uses: actions/setup-node@v1
        with:
          node-version: "12"

      - name: Setup
        working-directory: workspaces/eslint-config-dreamnet
        run: yarn install

      - name: Publish
        working-directory: workspaces/eslint-config-dreamnet
        run: yarn npm publish --tolerate-republish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
