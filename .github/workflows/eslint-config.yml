name: "eslint-config"

on:
  workflow_dispatch:
  push:
    tags: [ eslint-config-v* ]

jobs:
  # Publish on NPM
  publish:
    uses: ./.github/workflows/template.publish.yml
    with:
      checkout: true
      working-dir: workspaces/eslint-config
